import 'package:flutter/material.dart';

import '../../images.dart';

class MapWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Image.asset(Images.map);
  }
}
